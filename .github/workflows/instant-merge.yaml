name: Auto-Merge - ODH to RHODS

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - assigned
    paths:
      - README.MD

permissions:
  packages: write
  contents: read
  id-token: write

jobs:

  setup:
    runs-on: ubuntu-latest
    steps:
      - id: instant-merge
        run: |
          echo ${{ github.event }}
